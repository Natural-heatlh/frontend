#import '../Course/fragment.graphql'

query Courses {
  courses {
    id
    ...CourseFragment
  }
}

query Course($id: ID!) {
  course(id: $id) {
    ...CourseFragment
  }
}

mutation DeleteCourse($id: ID!) {
  deleteCourse(id: $id) {
    id
  }
}
