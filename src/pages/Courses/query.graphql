#import '../Course/fragment.graphql'

query CoursesQuery {
  courses {
    ...CourseFragment
  }
}

mutation BuyCourse($id: ID!) {
  buyCourse(id: $id) {
    email
    id
    courses {
      courseId
      progress
      isCompleted
    }
  }
}
